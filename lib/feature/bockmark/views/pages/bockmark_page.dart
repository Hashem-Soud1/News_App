import 'package:flutter/material.dart';

class BockmarkPage extends StatelessWidget {
  const BockmarkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bockmark')),
      body: Center(child: Text('Bockmark Page')),
    );
  }
}
